spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: Root@123
    url: jdbc:mysql://localhost:3306/order_data
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
